import {Note} from '../_component/note.jsx'

export const info = {
  author: [
    {github: 'johno', name: 'John Otander'},
    {github: 'wooorm', name: 'Titus Wormer'}
  ],
  modified: new Date('2025-01-27'),
  published: new Date('2018-08-11')
}
export const navSortSelf = 1

# MDX là gì?

Bài viết này giải thích định dạng MDX là gì.
Nó cho thấy cách markdown, JSX, biểu thức JavaScript, và các câu lệnh `import` và
`export` trong ESM có thể được sử dụng bên trong MDX. {/* more */}
Xem [§ Bắt đầu][start] để biết chi tiết về cách tích hợp MDX vào dự án
của bạn.
Xem [§ Sử dụng MDX][use] khi bạn đã sẵn sàng để sử dụng MDX.

## Nội dung

* [Điều kiện tiên quyết](#prerequisites)
* [Markdown cho kỷ nguyên component](#markdown-for-the-component-era)
* [Cú pháp MDX](#mdx-syntax)
  * [Markdown](#markdown)
  * [JSX](#jsx)
  * [Biểu thức](#expressions)
  * [ESM](#esm)
  * [Xen kẽ](#interleaving)
* [Đọc thêm](#further-reading)

## Điều kiện tiên quyết

Để viết và tận hưởng MDX, bạn nên quen thuộc với cả markdown
(xem [cheat sheet và hướng dẫn][commonmark-help] này để được trợ giúp) và
JavaScript (cụ thể là [JSX][jsx-spec]).

## Markdown cho kỷ nguyên component

MDX cho phép bạn sử dụng JSX trong nội dung markdown của mình.
Bạn có thể import các component, chẳng hạn như biểu đồ tương tác hoặc cảnh báo, và nhúng chúng
vào trong nội dung của bạn.
Điều này làm cho việc viết nội dung dài với các component trở nên tuyệt vời.
🚀

Thực tế hơn, MDX có thể được giải thích là một định dạng kết hợp markdown với
JSX và trông như sau:

```mdx
# Hello, world!

<div className="note">
  > Some notable things in a block quote!
</div>
```

Tiêu đề và block quote là markdown, trong khi những *thẻ giống HTML* đó là JSX.
Markdown thường cảm thấy tự nhiên hơn để gõ so với HTML hoặc JSX cho những thứ phổ biến
như nhấn mạnh hoặc tiêu đề.
JSX là một phần mở rộng cho JavaScript *trông* giống HTML nhưng làm cho việc
sử dụng các component (những thứ có thể tái sử dụng) trở nên thuận tiện.

Ví dụ này sử dụng `className` trên `<div>`.
Đó là vì nó được viết cho React và React mong đợi các class theo cách đó.
Các framework khác, chẳng hạn như Vue và Preact, mong đợi các class được định nghĩa
khác nhau, vì vậy lưu ý rằng có một số khác biệt trong cách JSX phải được
viết tùy thuộc vào công cụ nào được sử dụng với nó.

Một số tính năng khác từ JavaScript cũng được hỗ trợ trong MDX: biểu thức
trong dấu ngoặc nhọn (`{1 + 1}`) và ESM (`import` và `export`).

## Cú pháp MDX

<Note type="info">
  **Lưu ý**:
  Bạn không phải sử dụng cú pháp này với các gói `@mdx-js/*`.
  Hoặc luôn sử dụng nó.
  Nếu bạn đang sử dụng tích hợp bundler, bạn có thể thay đổi giữa MDX và
  markdown thông qua phần mở rộng file (`.mdx` vs. `.md`).
  Ngoài ra, `options.format` có thể được sử dụng.
</Note>

Cú pháp MDX kết hợp markdown với JSX.
Điều này mang lại cho chúng ta một cái gì đó theo hướng lập trình văn học.
Nó cũng mang lại cho chúng ta một sự kết hợp kỳ lạ của hai ngôn ngữ: markdown **nhạy cảm
với khoảng trắng** và **tha thứ** (những gì bạn gõ có thể không hoạt động chính xác nhưng nó
sẽ không crash) trong khi JavaScript **không nhạy cảm với khoảng trắng** và
**không tha thứ** (nó sẽ crash khi gõ sai).

Kỳ lạ thay, chúng tôi khá thích cách chúng kết hợp!

### Markdown

Markdown thường cảm thấy tự nhiên hơn để gõ so với HTML hoặc JSX cho những thứ phổ biến
như nhấn mạnh hoặc tiêu đề.
Markdown thường trông giống với những gì dự định hơn và ngắn gọn hơn.
Thay vì HTML sau:

```html
<blockquote>
  <p>A blockquote with <em>some</em> emphasis.</p>
</blockquote>
```

Bạn có thể viết tương đương trong markdown (hoặc MDX) như sau:

```md
> A blockquote with *some* emphasis.
```

MDX hỗ trợ markdown tiêu chuẩn theo mặc định ([CommonMark][]):

````md
# Heading (rank 1)
## Heading 2
### 3
#### 4
##### 5
###### 6

> Block quote

* Unordered
* List

1. Ordered
2. List

A paragraph, introducing a thematic break:

---

```js
some.code()
```

a [link](https://example.com), an ![image](./image.png), some *emphasis*,
something **strong**, and finally a little `code()`.
````

Các tính năng markdown không chuẩn (chẳng hạn như GFM, frontmatter, toán học, tô sáng
cú pháp) có thể được bật với các plugin (xem
[¶ Sử dụng plugins][using-plugins]).

Một số tính năng markdown không hoạt động trong MDX:

* Code thụt lề không hoạt động trong MDX:
  ```mdx
      console.log(1) // đây là một đoạn văn trong MDX!
  ```
  Lý do là để bạn có thể thụt lề các component của mình một cách đẹp mắt:
  ```mdx
  <main>
    <article>
      # Hello!
    </article>
  </main>
  ```
* Autolink không hoạt động trong MDX.
  Lý do là chúng có thể không phân biệt được với JSX (ví dụ:
  `<svg:rect>`) và chúng tôi thích rõ ràng.
  Nếu bạn muốn link, hãy sử dụng link đầy đủ:
  `[descriptive text](https://and-the-link-here.com)`
* Cú pháp HTML không hoạt động trong MDX vì nó được thay thế bằng JSX (`<img>` thành
  `<img />`).
  Thay vì comment HTML, bạn có thể sử dụng comment JavaScript trong dấu ngoặc nhọn:
  `{/* comment! */}`
* Dấu ngoặc nhọn trái / nhỏ hơn (`<`) và dấu ngoặc nhọn trái (`{`) không được escape
  phải được escape: `\<` hoặc `\{` (hoặc sử dụng biểu thức: `{'<'}`, `{'{'}`)`

Thêm về cách MDX khác với markdown được
[ghi chép ở đây](https://github.com/micromark/mdx-state-machine#72-deviations-from-markdown).

### JSX

[JSX][] là một phần mở rộng cho JavaScript *trông* giống HTML nhưng làm cho việc
sử dụng các component (những thứ có thể tái sử dụng) trở nên thuận tiện.
JSX thường được kết hợp với một framework frontend như React, Preact, hoặc Vue.
Các framework này thêm hỗ trợ cho các component, cho phép bạn thay đổi những thứ
lặp lại như markup sau:

```html
<h2>Hello, Venus!</h2>
<h2>Hello, Mars!</h2>
```

…thành JSX (hoặc MDX) như thế này:

```mdx
<Welcome name="Venus" />
<Welcome name="Mars" />
```

JSX tốt cho **components**.
Nó làm cho những thứ lặp lại rõ ràng hơn và cho phép tách biệt các mối quan tâm.
MDX hỗ trợ cú pháp JSX.
Sau đây trông rất giống HTML:

```mdx
<h1>Heading!</h1>

<abbr title="HyperText Markup Language">HTML</abbr> is a lovely language.

<section>
  And here is *markdown* in **JSX**!
</section>
```

Nhưng như đã đề cập trước đó, bạn cũng có thể sử dụng các component.
Lưu ý rằng các component phải được định nghĩa.
Bạn có thể import chúng, định nghĩa chúng cục bộ, hoặc truyền chúng vào sau (xem
[§ Sử dụng MDX][use]):

```mdx
<MyComponent id="123" />

You can also use objects with components, such as the `thisOne` component on
the `myComponents` object: <myComponents.thisOne />

<Component
  open
  x={1}
  label={'this is a string, *not* markdown!'}
  icon={<Icon />}
/>
```

Có một số trường hợp đặc biệt
[nơi MDX khác với JSX](https://github.com/micromark/mdx-state-machine#73-deviations-from-jsx).

### Biểu thức

MDX cũng hỗ trợ biểu thức JavaScript bên trong dấu ngoặc nhọn:

```mdx
Two 🍰 is: {Math.PI * 2}
```

Biểu thức có thể chứa toàn bộ chương trình JavaScript miễn là chúng
(được bao bọc trong) một biểu thức đánh giá thành một cái gì đó có thể được render.
Bạn có thể sử dụng một [IIFE][] như sau:

```mdx
{(function () {
  const guess = Math.random()

  if (guess > 0.66) {
    return <span style={{color: 'tomato'}}>Look at us.</span>
  }

  if (guess > 0.33) {
    return <span style={{color: 'violet'}}>Who would have guessed?!</span>
  }

  return <span style={{color: 'goldenrod'}}>Not me.</span>
})()}
```

Biểu thức có thể trống hoặc chỉ chứa một comment:

```mdx
{/* A comment! */}
```

### ESM

MDX hỗ trợ các câu lệnh `import` và `export` từ JavaScript.
Các tính năng ESM này có thể được sử dụng trong MDX để định nghĩa mọi thứ:

```mdx
import {External} from './some/place.js'

export const Local = properties => <span style={{color: 'red'}} {...properties} />

An <External>external</External> component and a <Local>local one</Local>.
```

ESM cũng có thể được sử dụng cho non-components (dữ liệu):

```mdx
import {Chart} from './chart.js'
import population from './population.js'
export const pi = 3.14

<Chart data={population} label={'Something with ' + pi} />
```

### Xen kẽ

Bạn có thể sử dụng markdown "inlines" nhưng không phải "blocks" bên trong JSX nếu văn bản và thẻ
ở trên cùng một dòng:

```mdx
<div># this is not a heading but *this* is emphasis</div>
```

Văn bản và thẻ trên một dòng không tạo ra blocks nên chúng không tạo ra `<p>`
nữa.
Trên các dòng riêng biệt, chúng làm:

```mdx
<div>
  This is a `p`.
</div>
```

Chúng tôi phân biệt bằng cách sử dụng quy tắc này (cùng dòng hay không).
Không dựa trên ngữ nghĩa của các phần tử trong HTML.
Vì vậy, bạn *có thể* xây dựng HTML không chính xác (mà bạn không nên):

```mdx
<h1 className="main">
  Don't do this: it's a `p` in an `h1`
</h1>

<h1 className="main">Do this: an `h1` with `code`</h1>
```

Không thể bao bọc "blocks" nếu văn bản và thẻ ở trên cùng một dòng nhưng
các thẻ mở và đóng tương ứng ở trong các block khác nhau
(vì vậy điều này không hợp lệ!):

```mdx-invalid chrome=no
Welcome! <a href="about.html">

This is home of...

# The Falcons!</a>
```

Để phân tích markdown, trước tiên chúng ta phải chia nó thành "blocks".
Vì vậy, trong trường hợp này là hai đoạn văn và một tiêu đề.
Để lại một thẻ `a` mở trong đoạn văn đầu tiên và một thẻ `a` đóng trong
tiêu đề.
Điều này gây ra lỗi vì nó bị lồng sai.

## Đọc thêm

* Nếu bạn muốn sử dụng MDX trong dự án của mình, xem [§ Bắt đầu][start];
  xem [§ Sử dụng MDX][use] khi bạn đã sẵn sàng
* Nếu bạn gặp lỗi khi làm việc với định dạng, xem
  [§ Khắc phục sự cố MDX][trouble]

[commonmark]: https://commonmark.org

[commonmark-help]: https://commonmark.org/help/

[iife]: https://developer.mozilla.org/en-US/docs/Glossary/IIFE

[jsx]: https://reactjs.org/docs/introducing-jsx.html

[jsx-spec]: https://facebook.github.io/jsx/

[start]: /docs/getting-started/

[trouble]: /docs/troubleshooting-mdx/

[use]: /docs/using-mdx/

[using-plugins]: /docs/extending-mdx/#using-plugins
