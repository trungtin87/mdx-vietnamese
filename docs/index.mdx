import {Chart} from './_component/snowfall.jsx'

export {Home as default} from './_component/home.jsx'
export const info = {
  author: [
    {github: 'johno', name: 'John Otander'},
    {github: 'wooorm', name: 'Titus Wormer'}
  ],
  modified: new Date('2025-01-27'),
  published: new Date('2017-12-23'),
  schemaOrg: {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "additionalType": "ComputerLanguage",
    "applicationCategory": "DeveloperApplication",
    "description": "an authorable format for writing JSX in markdown documents",
    "name": "MDX",
    "offers": {
      "@type": "Offer",
      "price": "0.00",
      "priceCurrency": "USD"
    },
    "operatingSystem": "Windows, MacOS, Linux",
    "sameAs": [
      "https://www.wikidata.org/wiki/Q95971592",
      "https://www.wikidata.org/wiki/Q27966906",
      "https://www.wikidata.org/wiki/Q95961071",
      "https://en.wikipedia.org/wiki/MDX_(markup_language)",
      "https://github.com/mdx-js/mdx"
    ],
    "url": "https://mdxjs.com"
  }
}
export const year = 2023

{/* lint disable heading-style */}

Markdown cho\
**kỷ nguyên component**
=================

MDX cho phép bạn sử dụng JSX trong nội dung markdown của mình.
Bạn có thể import các component, chẳng hạn như biểu đồ tương tác hoặc cảnh báo, và nhúng chúng
vào trong nội dung của bạn.
Điều này làm cho việc viết nội dung dài với các component trở nên tuyệt vời. {/* more */}
🚀
[Đọc tiếp »][what]

<div className="card big">
  ## Mới: MDX 3!

  Một bản cập nhật nhỏ lần này, không có gì lớn, điều này đôi khi cũng tốt!
  Phiên bản này chủ yếu ngừng hỗ trợ Node cũ (sử dụng 16 trở lên), thêm hỗ trợ ES2024
  hiện đại trong MDX, hỗ trợ `await` trong MDX (nếu framework của bạn cũng hỗ trợ), và
  loại bỏ một số tùy chọn đã lỗi thời.

  [Đọc tiếp »][v3]
</div>

## MDX làm gì?

<div className="big-columns">
  <div>
    Bạn viết markdown với các component được nhúng thông qua JSX:

    ```mdx path="example.mdx"
    import {Chart} from './snowfall.js'
    export const year = 2023

    # Last year's snowfall

    In {year}, the snowfall was above average.
    It was followed by a warm spring which caused
    flood conditions in many of the nearby rivers.

    <Chart color="#fcb32c" year={year} />
    ```
  </div>

  <div>
    Nó được biên dịch thành JavaScript mà bạn có thể sử dụng trong bất kỳ framework nào
    hỗ trợ JSX:

    {/* lint disable no-multiple-toplevel-headings */}

    <div className="home-preview">
      # Last year's snowfall

      In {year}, the snowfall was above average.
      It was followed by a warm spring which caused flood conditions in many of
      the nearby rivers.

      <Chart color="#fcb32c" year={year} />
    </div>

    {/* lint enable no-multiple-toplevel-headings */}
  </div>
</div>

Chúng tôi đã tạo một playground tương tác nơi bạn có thể thử MDX và xem nó
chuyển đổi thành gì.
[Chơi thử »][playground]

## Bắt đầu

Có các tích hợp cho hầu hết các bundler, framework và editor.
Dù bạn xây dựng với Docusaurus, Next.js, hay Vite.
Bạn thích Rollup, esbuild, hay webpack.
Bạn đang sử dụng React, Preact, hay Vue.
[Bắt đầu »][getting-started]

## MDX tóm tắt

<div className="emoji-list">
  * ❤️ **Mạnh mẽ**: MDX kết hợp cú pháp markdown và JSX để phù hợp hoàn hảo trong
    các dự án dựa trên JSX
  * 💻 **Mọi thứ đều là component**: Sử dụng các component hiện có trong
    MDX của bạn và import các file MDX khác dưới dạng component
  * 🔧 **Có thể tùy chỉnh**: Quyết định component nào được render cho mỗi cấu trúc
    markdown (`{h1: MyHeading}`)
  * 📚 **Dựa trên Markdown**: Sự đơn giản và thanh lịch của markdown vẫn còn,
    bạn chỉ sử dụng JSX khi muốn
  * 🔥 **Cực kỳ nhanh**: MDX không có runtime, tất cả quá trình biên dịch xảy ra
    trong giai đoạn build
</div>

> lol mdx is so good
>
> — **@dan\_abramov**

[getting-started]: /docs/getting-started/

[playground]: /playground/

[v3]: /blog/v3/

[what]: /docs/what-is-mdx/
